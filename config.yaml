env: "local" # local, dev, prod
storage_path: "postgres://postgres:mysecretpassword@localhost:5432/billing?sslmode=disable"
redis_addr: "localhost:6379"
http_server:
  address: "localhost:8080"
  timeout: 4s
  idle_timeout: 60s